# ============================================================
# RAG Agent Backend — Application Properties
# ============================================================

# --- Server ---
server.port=8080

# --- ChromaDB Configuration ---
# URL of the locally running ChromaDB instance
chromadb.url=http://localhost:8000
# Name of the ChromaDB collection to query
chromadb.collection=moroccan_knowledge
# Number of top document chunks to retrieve
chromadb.top-k=3

# --- Local LLM Configuration ---
# URL of the Apple MLX server running the fine-tuned LLaMA 3.1 model
# UPDATE THIS to your actual local network IP before running
llm.url=http://192.168.x.x:8080/v1/chat/completions
# Model identifier (sent in the API request body)
llm.model=llama-3.1-lora
# Max tokens for the LLM response
llm.max-tokens=1024
# Temperature for generation (lower = more deterministic)
llm.temperature=0.7

# --- Logging ---
logging.level.com.ragagent=DEBUG
